# Azure Cost Audit
Generated by Vikar

No cost waste detected in the three highest-impact categories checked.

1. **Unattached managed disks** — 0 found (no `az disk delete` needed).
2. **Orphaned public IPs** — 0 found (no `az network public-ip delete` needed).
3. **Stopped-but-not-deallocated VMs** — 0 found (no `az vm deallocate` needed).

Next steps: enable Azure Advisor cost recommendations and schedule monthly reviews for idle resources (e.g., low-CPU VMs, oversized disks, unused snapshots).
