# Azure Cost Audit
Generated by Vikar

No cost waste detected in the three scanned categories (unattached disks, orphaned public IPs, stopped-but-not-deallocated VMs). All counts are zero.

**Recommended next checks** (not in provided data):
1. **Idle VMs** (low CPU) – `az vm list -d --query "[?powerState=='VM running'].{name:name, resourceGroup:resourceGroup}" -o table`
2. **Oversized disks** (P30+ with low IOPS) – `az disk list --query "[?sku.name=='Premium_LRS' && diskSizeGb>=1024].{name:name, rg:resourceGroup, size:diskSizeGb}" -o table`
3. **Unused snapshots** older than 30 days – `az snapshot list --query "[?timeCreated<='2025-06-24'].{name:name, rg:resourceGroup}" -o table`
4. **ExpressRoute/VPN gateways** in unused VNets – `az network vnet-gateway list --query "[?enableBgp==\`false\`].{name:name, rg:resourceGroup}" -o table`
5. **Storage accounts** with no blob activity >90 days – `az storage account list --query "[?accessTier=='Hot'].{name:name, rg:resourceGroup}" -o table`
