# Azure Cost Audit
Generated by Vikar

Based on the provided JSON data, there appear to be no untagged managed disks or public IPs with attached NICs. However, stopped VMs are still billing for compute resources.

1. Deallocate unused VM instances:
   - Reason: Stoped VMs consuming unnecessary costs.
   ```az vm deallocate --resource-group myResourceGroup --name myVM```

2. Review and remove any other untagged or unattached disks if found in future audits to prevent unexpected charges.

3. Set a budget for the subscription monitoring Azure Cost Management + Billing reports:
   - Reason: Proactive budgeting helps avoid cost overruns.
   ```az consumption.reset-account --subscription-id <SubscriptionId>```

4. Evaluate VM scaling policies, autoscale settings or scheduled shutdowns if no longer needed to reduce unnecessary usage.

5. Implement tags and budget limits for resource grouping by purpose/environments:
   - Reason: Avoiding unintentional cost drift due to mismanagement of resources.
   ```az group update --name myResourceGroup --tags Department=Finance```

Note, the exact Azure Resource Groups (myResourceGroup) should be replaced in each command. There are no public IPs listed but it is always good practice for a regular checkup on other unattached NIC related charges if any appear after future audits. If there were managed disks with attached resources found during an audit it's recommended to remove them as well using the following az commands:

```az disk delete --name myDiskName --resource-group myResourceGroup```
or
```az network nic reset-ipconfig -g MyResourceGroup --name myNicName  --ip-address 'xxx.xxx.xxx.xxx' ```
